#include <stdio.h>
int main()
{
 	int bin1,bin2;
    int i=0,rem=0,sum[20];
     printf("Enter the first binary number: ");
    scanf("%ld", &bin1);
    printf("Enter the second binary number: ");
    scanf("%ld", &bin2);
	 
}
