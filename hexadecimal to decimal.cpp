/* program to convert hexadecimal to decimal*/
#include<stdio.h>
int main()
{
	int num;
	printf("Enter the number in hexadeimal:");
	scanf("%x",&num);
	printf("Hexadecimal of %x is %d",num,num);
}
